#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# ログファイルをクリア
echo "🧹 コミット前にログをクリアしています..."
pnpm run log:clear

# TypeScriptとLintのエラーチェック
echo "🔍 TypeScriptエラーをチェックしています..."
pnpm run check:wsl

# リファクタリングチェック
bash .claude/hooks/refactor-check.sh

pnpm lint-staged
