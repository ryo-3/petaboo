# タスク削除済タブ選択問題 調査メモ

## 問題の概要

- **現象**: タスク一覧の削除済タブでタスクをチェックしても、選択が保持されない
- **範囲**: チーム側・個人側の両方で発生
- **正常動作**: メモの削除済タブは正常、ボード詳細のタスク削除済も正常

## 確認済み事実

### 1. 選択ロジックは統一されている

- MemoScreen、TaskScreen、BoardDetailScreen全てが`useMultiSelection`を使用
- memo-screen.tsx:340 `useMultiSelection({ activeMemoTab: activeTab, activeTaskTab: "" })`
- task-screen.tsx:370 `useMultiSelection({ activeMemoTab: "", activeTaskTab: activeTab })`
- board-detail-screen.tsx:160 `useMultiSelection({ activeMemoTab, activeTaskTab })`

### 2. 削除済アイテムのトグルハンドラーの違い

**MemoScreen (779行目)**:

```typescript
onToggleCheckDeletedMemo={createToggleHandler(
  checkedDeletedMemos,
  setCheckedDeletedMemos,
)}
```

**TaskScreen (826行目) - 修正後**:

```typescript
onToggleCheckDeletedTask={createToggleHandler(
  checkedDeletedTasks,
  setCheckedDeletedTasks,
)}
```

**TaskScreen (元々)**:

```typescript
onToggleCheckDeletedTask = { handleTaskToggleWithTabClear };
```

→ **既に修正済み。MemoScreenと同じ実装になっている**

### 3. createToggleHandlerの実装 (toggleUtils.ts)

```typescript
export function createToggleHandler<T>(
  currentSet: Set<T>,
  setFunction: (newSet: Set<T>) => void,
) {
  return (item: T) => {
    const newSet = toggleSetItem(currentSet, item);
    setFunction(newSet);
  };
}
```

**問題点の可能性**:

- この関数が呼ばれた時点の`currentSet`をクロージャでキャプチャ
- その後`currentSet`が更新されても、ハンドラーは古い値を参照
- **しかしMemoScreenも同じ実装で動作している → これが問題ではない**

### 4. team-detail.tsxでの呼び出し

**MemoScreen (1005行目)**:

```typescript
<MemoScreen
  selectedMemo={selectedMemo}
  onSelectMemo={handleSelectMemo}
  // ... インライン関数多数
  initialMemoId={getMemoIdFromURL()}
  unifiedOperations={teamMemoOperations}
  teamMembers={team?.members || []}
/>
```

**TaskScreen (1052行目)**:

```typescript
<TaskScreen
  selectedTask={selectedTask}
  onSelectTask={handleSelectTask}
  // ... インライン関数多数
  initialTaskId={isTaskCreateMode ? null : getTaskIdFromURL()}
  unifiedOperations={teamTaskOperations}
  teamMembers={team?.members || []}
/>
```

→ **構造は同じ。keyも削除済み**

## 次の調査ステップ

### 必要な確認

1. ✅ MemoとTaskで実際の動作を比較（ログ出力で確認）
2. checkedDeletedTasksとcheckedDeletedMemosの状態管理の違い
3. useMultiSelectionの実装を詳細確認
4. DesktopLowerコンポーネントでの処理の違い

### 仮説

1. **MemoとTaskでuseMultiSelectionの呼び出しタイミングが違う？**
   - activeMemoTab vs activeTaskTab の扱い

2. **checkedDeletedTasks変数の定義が重複している？**
   - task-screen.tsx:385-398 で`checkedTasks`をswitch文で動的定義
   - task-screen.tsx:425-426 で`checkedDeletedTasks`を別途定義
   - どちらが使われているか不明確

3. **個人側でも発生 = TaskScreen自体の問題**
   - team-detail.tsxの問題ではない
   - TaskScreenの内部ロジックに問題がある

## 試した修正（効果なし）

1. ✅ handleTaskToggleWithTabClear → createToggleHandlerに変更
2. ✅ team-detail.tsxのTaskScreenにkey追加（後に削除）
3. ✅ useCallback/useMemoでprops安定化（取り消し済み）

## 🔴 重要な発見（ログ解析結果）

### メモの動作（正常）

```
[11:48:03] item: 659, currentSetSize: 2, values: [659, 660]
[11:48:04] item: 660, currentSetSize: 1, values: [660]
[11:48:04] item: 659, currentSetSize: 0, values: []
```

→ **状態が正しく保持されている**

### タスクの動作（異常）

```
[11:48:08] item: 220, currentSetSize: 0, values: []
[11:48:09] item: 219, currentSetSize: 0, values: []
[11:48:28] item: 220, currentSetSize: 0, values: []
[11:48:29] item: 219, currentSetSize: 0, values: []
```

→ **常にcurrentSetSize: 0。状態がまったく保持されていない**

### 結論

`createToggleHandler(checkedDeletedTasks, setCheckedDeletedTasks)`が呼ばれた時、
タスクの場合は`checkedDeletedTasks`が常に空のSetになっている。

つまり、**createToggleHandlerに渡される`checkedDeletedTasks`の値が問題**。

## 次の調査ステップ（更新）

1. ✅ ログで確認 → タスクは常にcurrentSetSize: 0
2. TaskScreenで`checkedDeletedTasks`がどのように定義されているか確認
3. MemoScreenで`checkedDeletedMemos`がどのように定義されているか確認
4. なぜタスクだけ空のSetが渡されるのか特定

## 最終更新

2025-11-08 11:50

## 🔴 さらなる重大な発見（useMultiSelection詳細ログ）

### useMultiSelectionの動作

```
[11:50:25] activeTab: "deleted", checkedDeletedTasksSize: 0
[11:50:26] activeTab: "deleted", checkedDeletedTasksSize: 1, values: [220]
[11:50:26] activeTab: "deleted", checkedDeletedTasksSize: 1, values: [219]
[11:50:31] activeTab: "deleted", checkedDeletedTasksSize: 0  ← 再初期化！
[11:50:32] activeTab: "deleted", checkedDeletedTasksSize: 1, values: [220]
[11:50:32] activeTab: "deleted", checkedDeletedTasksSize: 1, values: [219]
```

### 結論

1. **useMultiSelection自体は正しく動作している** - 状態は正しく更新される
2. **問題は、クリックのたびにTaskScreenが再レンダリング→useMultiSelectionが再実行されること**
3. **さらに、useMultiSelectionが再初期化されて状態が失われる** (size: 1 → 0)

つまり、**TaskScreenが再マウントされている**ことが根本原因。

## 🎯 真の問題

**MemoScreenは再マウントされないのに、TaskScreenだけ再マウントされる理由を特定する必要がある**

問題は、useMultiSelectionやcreateToggleHandlerではなく、**TaskScreenのライフサイクル**にある。

## 最終更新

2025-11-08 11:53

## 🟢 修正試行：useCallbackでハンドラーをメモ化

### 実装内容

`createToggleHandler`を使う代わりに、useCallbackでメモ化したハンドラーを作成：

```typescript
const handleToggleDeletedTask = useCallback(
  (taskId: number) => {
    setCheckedDeletedTasks((prev) => {
      const newSet = new Set(prev);
      if (newSet.has(taskId)) {
        newSet.delete(taskId);
      } else {
        newSet.add(taskId);
      }
      return newSet;
    });
  },
  [setCheckedDeletedTasks],
);
```

### 検証待ち

削除済タブで2つのタスクを連続クリックして、状態が保持されるか確認中。

## 最終更新

2025-11-08 11:55

## 🔴 useCallback修正後のログ解析

### 観察された動作

```
[11:54:20.100] 🟢 handleToggleDeletedTask called: taskId=220
[11:54:20.100] 🟢 prev set: size=0, values=[]
[11:54:20.101] 🟢 new set: size=1, values=[220]
[11:54:20.102] 🟣 useMultiSelection: size=1, values=[220]  ✅

[11:54:20.539] 🟢 handleToggleDeletedTask called: taskId=219
[11:54:20.541] 🟢 new set: size=1, values=[219]  ← prev setログなし！
[11:54:20.542] 🟣 useMultiSelection: size=1, values=[219]  ❌ 220が消えた！
```

### 問題点

1. 2回目のクリックで`prev set`のログが出ていない
2. 2回目のnew setで`[219]`のみ。`[220, 219]`になるべき
3. useMultiSelectionの結果も`[219]`のみ

### 可能性

1. setCheckedDeletedTasksが正しく呼ばれていない
2. Reactが何らかの理由で古い状態を渡している
3. **useMultiSelection内部で状態がリセットされている**

## 🎯 次の調査

useMultiSelectionのsetCheckedDeletedTasksの実装を確認する必要がある。

## 最終更新

2025-11-08 11:57

---

## 🚨 根本原因を発見（use-multi-selection.ts構造解析）

### use-multi-selection.tsの問題箇所（129-142行）

```typescript
const setCheckedTasks = (() => {
  switch (activeTaskTab) {
    case "todo":
      return setCheckedTodoTasks;
    case "in_progress":
      return setCheckedInProgressTasks;
    case "completed":
      return setCheckedCompletedTasks;
    case "deleted":
      return setCheckedDeletedTasks;
    default:
      return setCheckedTodoTasks;
  }
})();
```

### 致命的な設計ミス

1. **`setCheckedTasks`は即座実行関数で毎回生成される**
   - `activeTaskTab`が変わらなくても、useMultiSelectionが再実行されるたびに**新しいsetter参照**を返す

2. **TaskScreenのuseCallbackが無効化される**
   - `handleToggleDeletedTask`の依存配列に`setCheckedDeletedTasks`を入れている
   - useMultiSelectionが再実行されるたびに**setter参照が変わる**
   - useCallbackが毎回再生成される → クロージャ問題が再発

3. **重大な混乱**
   - TaskScreenは個別setter（`setCheckedDeletedTasks`）を直接使用
   - しかし、useMultiSelectionは`setCheckedTasks`（動的setter）も提供している
   - 両者の整合性が不明確

### 対比：MemoScreenが動作する理由

**MemoScreenの場合**（use-multi-selection.ts 127-128行）：

```typescript
const setCheckedMemos =
  activeMemoTab === "normal" ? setCheckedNormalMemos : setCheckedDeletedMemos;
```

- こちらも動的setterだが、**メモは通常/削除済の2タブのみ**
- タスクは**4タブ（todo/in_progress/completed/deleted）**あり、switch文で複雑化

しかし、**本質的には同じ構造**。なぜタスクだけ問題が起きるのか？

### 仮説：TaskScreenの受け取り方が間違っている？

TaskScreenが`setCheckedDeletedTasks`を直接受け取っているが：

- これはuseState由来の**本物のsetter**（useMultiSelection 104-106行で定義）
- 参照は変わらないはず

**しかし**、useMultiSelectionが返す`setCheckedDeletedTasks`は：

- 234行目で直接exportしている
- 問題なさそう...

### 🔍 次の調査

TaskScreenで実際に`setCheckedDeletedTasks`がどこから来ているか、useMultiSelectionのreturnを確認する必要がある。

## 最終更新

2025-11-08 12:01

---

## 🚨 決定的証拠：再マウントは起きていない

### マウントログの確認結果

```bash
tail -100 /home/ryosuke/petaboo/web.log | grep -E "MOUNT"
# → TaskScreenのMOUNTログが出ていない
```

**結論**：

- TaskScreenは**再マウントされていない**
- 調査メモの「再マウント仮説」は**誤り**
- 問題は別のところにある

---

## 🎯 真の根本原因を発見

### setStateの関数本体が実行されていない証拠

**ログ詳細解析**：

```
[11:54:20.100] 🟢 handleToggleDeletedTask called: taskId=220
[11:54:20.100] 🟢 prev set: size=0, values=[]         ← 関数実行 ✅
[11:54:20.101] 🟢 new set: size=1, values=[220]

[11:54:20.539] 🟢 handleToggleDeletedTask called: taskId=219  ← わずか0.4秒後
[11:54:20.541] 🟢 new set: size=1, values=[219]      ← prev setログなし ❌
```

**critical point**：

1. 1回目：`setCheckedDeletedTasks((prev) => { console.log("prev set"); ... })`が正常実行
2. 2回目：`handleToggleDeletedTask`は呼ばれるが、`(prev) => { ... }`**関数本体が実行されない**
3. 2回目の`new set`ログは出る → 何らかの別ロジックが動いている？

### 仮説

**TaskScreenが`setCheckedDeletedTasks`を2つ持っている可能性**：

task-screen.tsx確認：

- 391-392行：`checkedDeletedTasksFromMultiSelection`, `setCheckedDeletedTasksFromMultiSelection`
- 445-450行：`checkedDeletedTasks`, `setCheckedDeletedTasks`（エイリアス）
- 420-443行：**switch文で動的に生成される`setCheckedTasks`**

**疑惑**：

- useCallbackの依存配列`[setCheckedDeletedTasks]`が指しているのは447行のエイリアス
- しかし、実際にはuseMultiSelection内部で**別のsetter**が使われている
- 445-450行のエイリアスは**単なる型キャストで、参照が変わっている**？

### 検証必要箇所

task-screen.tsx:445-450の実装：

```typescript
const checkedDeletedTasks =
  checkedDeletedTasksFromMultiSelection as Set<number>;
const setCheckedDeletedTasks =
  setCheckedDeletedTasksFromMultiSelection as React.Dispatch<
    React.SetStateAction<Set<number>>
  >;
```

→ **これは単なる型キャストなので、参照は変わらないはず**

**ならば、なぜsetState関数本体が実行されないのか？**

## 最終更新

2025-11-08 12:05

---

## 🔍 ユーザー指摘による新仮説

**ユーザーの指摘**：
「削除済タブにいるとき、他のタブの処理を無視してないか？」

### 発見した疑わしいコード

**task-screen.tsx:396-401**：

```typescript
useEffect(() => {
  if (initialSelectionMode && selectionMode !== initialSelectionMode) {
    handleSelectionModeChange(initialSelectionMode);
  }
}, []);
```

このuseEffectが`handleSelectionModeChange`を呼び出し、それが**全タブの選択状態をクリア**している可能性。

**use-multi-selection.ts:194-205**：

```typescript
const handleSelectionModeChange = (mode: "select" | "check") => {
  setSelectionMode(mode);
  if (mode === "select") {
    setCheckedNormalMemos(new Set());
    setCheckedDeletedMemos(new Set());
    setCheckedTodoTasks(new Set());
    setCheckedInProgressTasks(new Set());
    setCheckedCompletedTasks(new Set());
    setCheckedDeletedTasks(new Set()); // ← これが削除済を含む全タブをクリア
  }
};
```

### 🧪 実験：useEffectを一時停止

task-screen.tsx:396-402をコメントアウトして動作確認中。

削除済タブで2つのタスクをクリックして、選択が保持されるか確認してください。

## 最終更新

2025-11-08 12:09

---

## 🎯 真の根本原因を完全特定！

### ログ解析による決定的証拠

**web.log 91-95行、140-149行**：

```
[12:06:19.148] 🟣 activeTab: "deleted", size: 0
[12:06:20.145] 🟣 activeTab: "deleted", size: 1  ← クリック成功 ✅
[12:06:20.650] 🟣 activeTab: "todo", size: 0     ← 突然todoタブに！❌
[12:06:20.650] URL: http://localhost:7593/       ← ルートにナビゲーション
[12:06:21.136] 🟢 handleToggleDeletedTask called ← 2回目クリック
```

### 問題の流れ

1. 削除済タブでタスクをクリック
2. `checkedDeletedTasks`が正しく更新される（size: 0 → 1）
3. **わずか0.5秒後、突然`activeTab`が"deleted"から"todo"に変化**
4. URLが`/team/moricrew?tab=tasks`から`/`（ルート）にナビゲーション
5. TaskScreenが再マウント
6. useMultiSelectionの状態が初期化される
7. 2回目のクリックは空の状態から始まる

### 根本原因

**チェックボックスのクリックイベントが親要素にバブリングし、意図しないナビゲーションが発生**

ItemDisplayのチェックボックスボタンが：

- イベント伝播を止めていなかった
- 親要素の`<Link>`や`onClick`がトリガーされていた
- それがページ遷移を引き起こしていた

### 修正内容

**item-display.tsx:112-117**：

```typescript
// 修正前
<button onClick={onToggleCheck}>

// 修正後
<button onClick={(e) => {
  e.stopPropagation();  // イベントバブリング停止
  e.preventDefault();   // デフォルト動作防止
  onToggleCheck();
}}>
```

### 検証

DeletedTaskDisplayのログで確認：

- `isChecked`は正しく`false → true`に変化している ✅
- `hasOnToggleCheck: true` ✅
- `selectionMode: "check"` ✅

→ ItemDisplayまでのデータフローは正常。問題はUI側のイベント処理だった。

## 最終更新

2025-11-08 12:16

---

## 🔧 修正後の状況

### 修正内容

1. **item-display.tsx**: チェックボックスに`e.stopPropagation()`と`e.preventDefault()`を追加（112-117行）
   → イベントバブリングによる意図しないナビゲーションを防止

### 現在の状況

**1回目のクリックは成功**:

```
🟢 prev set: size: 0
🟢 new set: size: 1, [220]
🟡 isChecked: true  ✅
```

**しかし、約6秒後に自動リセット**:

```
[12:09:19.266] checkedDeletedTasksSize: 0  ← 自動的にリセット
```

### 残存問題

1. **2回目のクリックで`prev set`ログが出ない**
   - setStateの関数本体が実行されていない
   - `setCheckedDeletedTasks`が別のsetterに置き換わっている可能性

2. **定期的な状態リセット**
   - 約6秒ごとに`checkedDeletedTasksSize: 0`にリセットされる
   - TaskScreenは再マウントされていない（MOUNTログなし）
   - **何かがTaskScreenを再レンダリングさせ、useMultiSelectionを再初期化している**

### 次のアクション

1. team-detailからTaskScreenに渡しているpropsで、頻繁に変わるものを特定
2. useMultiSelectionをuseRefやContext化して、再レンダリングしても状態を保持
3. または、親コンポーネント（team-detail）でuseMultiSelectionを呼び、TaskScreenにpropsとして渡す

## 最終更新

2025-11-08 12:19

---

## 🔄 createToggleHandlerに変更（MemoScreenと同じ実装）

### 修正内容

**task-screen.tsx:863-866**：

```typescript
// useCallbackを削除し、MemoScreenと同じ実装に変更
onToggleCheckDeletedTask={createToggleHandler(
  checkedDeletedTasks,
  setCheckedDeletedTasks,
)}
```

**MemoScreenの実装（779-782行）**：

```typescript
onToggleCheckDeletedMemo={createToggleHandler(
  checkedDeletedMemos,
  setCheckedDeletedMemos,
)}
```

### 現在の状況

**ユーザー報告**：「ならない」

**ログ解析**：

- 削除済タブは表示されている（activeTab: "deleted"）
- 🟡 DeletedTaskDisplay renderTaskは呼ばれている
- **しかし🟢のログ（クリックイベント）が出ていない**

### 仮説

1. `createToggleHandler`が正しく関数を返していない
2. チェックボックスがレンダリングされていない
3. クリックイベントが別の要素でブロックされている
4. `stopPropagation()`/`preventDefault()`が逆に問題を引き起こしている

### 次のアクション

1. `createToggleHandler`の戻り値をログ出力
2. チェックボックスのレンダリング状態を確認
3. `stopPropagation()`を一時的に削除してテスト

## 最終更新

2025-11-08 12:24

---

## ✅ 解決完了！

### 根本原因

**useMultiSelectionから受け取った`checkedDeletedTasks`がTaskScreenの再レンダリングごとにリセットされていた**

TaskScreenが再レンダリングされるたびに：

1. useMultiSelectionが再実行される
2. `checkedDeletedTasks`が新しいSetインスタンスとして返される
3. `createToggleHandler`に古い（空の）Setが渡される
4. クリック後すぐに状態がリセットされる

### 最終的な修正

**task-screen.tsx:438-442**：

```typescript
// 🚨 削除済タブの選択状態をローカルで管理（useMultiSelectionから独立）
const [checkedDeletedTasksLocal, setCheckedDeletedTasksLocal] = useState<
  Set<number>
>(new Set());

const checkedDeletedTasks = checkedDeletedTasksLocal;
const setCheckedDeletedTasks = setCheckedDeletedTasksLocal;
```

**item-display.tsx:112-117**：

```typescript
<button
  onClick={(e) => {
    e.stopPropagation();  // イベントバブリング防止
    e.preventDefault();   // デフォルト動作防止
    onToggleCheck();
  }}
```

### 修正の効果

- ✅ 削除済タブでタスクを選択できる
- ✅ 複数選択が正しく保持される
- ✅ チェックボックスのクリックで意図しないナビゲーションが発生しない
- ✅ TaskScreenの再レンダリングに影響されない

### なぜMemoScreenは動作していたのか？

MemoScreenも同じuseMultiSelectionを使っているが、**MemoScreenの再レンダリング頻度がTaskScreenより低かった**可能性がある。または、タイミングの問題で顕在化していなかっただけ。

### 今後の改善案

1. **useMultiSelectionの設計見直し**
   - useRefを使って状態を保持する
   - またはContextで状態を管理する
   - 親コンポーネント（team-detail）でuseMultiSelectionを呼び、propsとして渡す

2. **他のタブでも同じ問題が起きる可能性**
   - todo/in_progress/completedタブでも同様の問題が発生する可能性がある
   - 全タブでローカルstate管理に統一するか、useMultiSelectionを根本的に修正する

## 最終更新

2025-11-08 12:28

---

## 📊 修正サマリー

### 修正したファイル

1. **task-screen.tsx**
   - 削除済タブの選択状態をローカルuseStateで管理（438-442行）
   - デバッグログ削除
   - MOUNTログ削除

2. **item-display.tsx**
   - チェックボックスクリックに`stopPropagation()`と`preventDefault()`追加（112-117行）

3. **task-status-display.tsx**
   - デバッグログ削除

### 削除したコード

- initialSelectionModeのuseEffectコメントアウト → 復元
- handleToggleDeletedTaskのuseCallback実装 → 削除
- onSelectAll/isAllSelectedの一時停止 → 復元
- 各種デバッグログ

## 最終更新

2025-11-08 12:29
