# 個人ボード詳細 選択モード対応 Plan

> ⚠️ Codexに実装依頼する際は、以下を厳守すること：
>
> - **既存ファイルを丸ごと再生成させないこと**
>   → Codexへの依頼は必ず **差分（patch形式）** で行う
> - **日本語コメント・文字列を扱う場合はUTF-8前提で依頼すること**
>   → 文字化け防止のため明記する
> - **Codexに git add / git commit を実行させないこと**
> - **完了した場合ファイルを`.claude/fixed-plans` に移動する**

---

## 📋 目的

個人ボード詳細画面で、アイテム（メモ/タスク）が選択された状態の時に、ヘッダーコントロールパネルの表示を適切に切り替える。

### 現状の問題

- アイテムを選択しても、ヘッダーのContentFilterが通常モードのまま
- ツールチップが「メモを非表示」「タスクを非表示」のまま
- ボタンアイコンが通常モード（メモアイコン・タスクアイコン）のまま

### 期待される動作

- アイテム選択時は選択モードに切り替わる
- ツールチップが「一覧パネルを表示/非表示」「詳細パネルを表示/非表示」に変わる
- ボタンアイコンが「一覧アイコン」「選択中のアイテムタイプのアイコン」に変わる

---

## 🔍 参考実装

チームボード詳細（3パネル版）では既に実装済み：

- ファイル: `apps/web/components/screens/board-detail-screen-3panel.tsx`
- 行: 616-654

```typescript
const shouldShowPanelControls =
  teamMode && !rightPanelMode && (selectedMemo || selectedTask);
const selectedItemType = selectedMemo ? "memo" : selectedTask ? "task" : null;

// ...

if (shouldShowPanelControls) {
  config.isSelectedMode = true;
  config.showMemo = showListPanel;
  config.showTask = showDetailPanel;
  config.showComment = showCommentPanel;
  config.onMemoToggle = handleListPanelToggle;
  config.onTaskToggle = handleDetailPanelToggle;
  config.onCommentToggle = handleCommentPanelToggle;
  config.contentFilterRightPanelMode = rightPanelMode;
  config.listTooltip = showListPanel
    ? "一覧パネルを非表示"
    : "一覧パネルを表示";
  config.detailTooltip = showDetailPanel
    ? "詳細パネルを非表示"
    : "詳細パネルを表示";
  config.selectedItemType = selectedItemType;
}
```

---

## 📝 変更範囲

### 対象ファイル

- `apps/web/components/screens/board-detail-screen.tsx`

### 変更内容

1. **選択モード判定の追加**（行627-634付近）

```typescript
// 追加
const shouldShowSelectedMode =
  !rightPanelMode && (selectedMemo || selectedTask);
const selectedItemType = selectedMemo ? "memo" : selectedTask ? "task" : null;
```

2. **boardHeaderConfig の条件分岐追加**（行636-665付近）

現在の実装:

```typescript
const boardHeaderConfig = useMemo<HeaderControlPanelConfig | null>(() => {
  const config: HeaderControlPanelConfig = {
    currentMode: "board",
    rightPanelMode: headerRightPanelMode,
    boardId,
    onBoardSettings: boardSettingsHandler,
    onBoardExport: handleExport,
    isExportDisabled: false,
    showMemo: headerShowMemo,
    showTask: headerShowTask,
    onMemoToggle: handleMemoToggle,
    onTaskToggle: handleTaskToggle,
    contentFilterRightPanelMode: rightPanelMode,
    selectionMode,
    onSelectionModeChange: handleSelectionModeChange,
    onCsvImport: handleCsvImport,
    customTitle: boardName || "ボード詳細",
    normalCount: totalNormalCount,
    completedCount,
    deletedCount: totalDeletedCount,
    teamMode,
    teamId: teamId ?? undefined,
  };

  if (teamMode) {
    config.showComment = showComment;
    config.onCommentToggle = handleCommentToggle;
  }

  return config;
  // ...
}, [dependencies]);
```

修正後:

```typescript
const boardHeaderConfig = useMemo<HeaderControlPanelConfig | null>(() => {
  const config: HeaderControlPanelConfig = {
    currentMode: "board",
    rightPanelMode: headerRightPanelMode,
    boardId,
    onBoardSettings: boardSettingsHandler,
    onBoardExport: handleExport,
    isExportDisabled: false,
    selectionMode,
    onSelectionModeChange: handleSelectionModeChange,
    onCsvImport: handleCsvImport,
    customTitle: boardName || "ボード詳細",
    normalCount: totalNormalCount,
    completedCount,
    deletedCount: totalDeletedCount,
    teamMode,
    teamId: teamId ?? undefined,
  };

  // アイテム選択時のパネル制御モード
  if (shouldShowSelectedMode) {
    config.isSelectedMode = true;
    config.showMemo = showMemo;
    config.showTask = showTask;
    config.onMemoToggle = handleMemoToggle;
    config.onTaskToggle = handleTaskToggle;
    config.contentFilterRightPanelMode = rightPanelMode;
    config.listTooltip = showMemo ? "一覧パネルを非表示" : "一覧パネルを表示";
    config.detailTooltip = showTask ? "詳細パネルを非表示" : "詳細パネルを表示";
    config.selectedItemType = selectedItemType;
  } else {
    // 通常モード
    config.showMemo = headerShowMemo;
    config.showTask = headerShowTask;
    config.onMemoToggle = handleMemoToggle;
    config.onTaskToggle = handleTaskToggle;
    config.contentFilterRightPanelMode = rightPanelMode;
  }

  if (teamMode) {
    config.showComment = showComment;
    config.onCommentToggle = handleCommentToggle;
  }

  return config;
  // ...
}, [dependencies]);
```

3. **依存配列の更新**（行667-688付近）

依存配列に以下を追加:

- `shouldShowSelectedMode`
- `selectedItemType`
- `showMemo`
- `showTask`

---

## 🔧 実装手順

### Step 1: 選択モード判定変数の追加

`board-detail-screen.tsx` の 627-634行付近（`boardHeaderConfig` の直前）に以下を追加:

```typescript
const shouldShowSelectedMode =
  !rightPanelMode && (selectedMemo || selectedTask);
const selectedItemType = selectedMemo ? "memo" : selectedTask ? "task" : null;
```

### Step 2: boardHeaderConfig の条件分岐実装

`boardHeaderConfig` の useMemo 内を以下のように修正:

1. 基本設定から `showMemo`, `showTask`, `onMemoToggle`, `onTaskToggle`, `contentFilterRightPanelMode` を削除（条件分岐で設定するため）

2. `if (teamMode)` の前に以下の条件分岐を追加:

```typescript
// アイテム選択時のパネル制御モード
if (shouldShowSelectedMode) {
  config.isSelectedMode = true;
  config.showMemo = showMemo;
  config.showTask = showTask;
  config.onMemoToggle = handleMemoToggle;
  config.onTaskToggle = handleTaskToggle;
  config.contentFilterRightPanelMode = rightPanelMode;
  config.listTooltip = showMemo ? "一覧パネルを非表示" : "一覧パネルを表示";
  config.detailTooltip = showTask ? "詳細パネルを非表示" : "詳細パネルを表示";
  config.selectedItemType = selectedItemType;
} else {
  // 通常モード
  config.showMemo = headerShowMemo;
  config.showTask = headerShowTask;
  config.onMemoToggle = handleMemoToggle;
  config.onTaskToggle = handleTaskToggle;
  config.contentFilterRightPanelMode = rightPanelMode;
}
```

### Step 3: 依存配列の更新

useMemo の依存配列に以下を追加:

```typescript
shouldShowSelectedMode,
selectedItemType,
showMemo,
showTask,
```

---

## ✅ 動作確認チェックリスト

- [ ] 個人ボード詳細でメモを選択した時、ヘッダーのボタンが選択モードに切り替わる
- [ ] 個人ボード詳細でタスクを選択した時、ヘッダーのボタンが選択モードに切り替わる
- [ ] ツールチップが「一覧パネルを表示/非表示」「詳細パネルを表示/非表示」に変わる
- [ ] 左ボタンのアイコンが「一覧（List）アイコン」に変わる
- [ ] 右ボタンのアイコンが選択中のアイテムタイプ（メモ/タスク）に応じたアイコンに変わる
- [ ] 選択解除時に通常モードに戻る
- [ ] デスクトップ表示で正常動作する
- [ ] モバイル表示で正常動作する（md: 768px以下）
- [ ] チーム側の既存実装に影響がない

---

## 🚨 注意点

1. **個人とチームの構造の違い**
   - チーム側（3panel）: `showListPanel`, `showDetailPanel`, `showCommentPanel` の3パネル制御
   - 個人側（2panel）: `showMemo`, `showTask` の2パネル制御
   - ツールチップのテキストは同じだが、パネルの意味が異なる点に注意

2. **rightPanelMode の考慮**
   - `rightPanelMode` が設定されている場合（memo-list/task-list）は選択モードを表示しない
   - これはチーム側と同じロジック

3. **モバイル対応**
   - モバイル時は `ContentFilter` が非表示（`hidden md:flex`）
   - しかし内部ロジックは正しく動作する必要がある

---

## 📊 影響範囲

### 直接影響

- `apps/web/components/screens/board-detail-screen.tsx`

### 間接影響（確認必要）

- `apps/web/components/ui/controls/header-control-panel.tsx` - 影響なし（受け取る側）
- `apps/web/components/ui/controls/content-filter.tsx` - 影響なし（受け取る側）
- `apps/web/src/contexts/header-control-panel-context.tsx` - 影響なし（型定義のみ）

---

## 🎯 Codex用 ToDoリスト

### タスク1: 選択モード判定変数の追加

**ファイル**: `apps/web/components/screens/board-detail-screen.tsx`
**位置**: 627-634行付近（`boardHeaderConfig` の直前）

**変更内容**:

```diff
  const headerRightPanelMode = (
    selectedMemo || selectedTask || rightPanelMode ? "view" : "hidden"
  ) as "hidden" | "view" | "create";
  const totalNormalCount = allMemoItems.length + allTaskItems.length;
  const totalDeletedCount = deletedCount + deletedMemoCount;
+ const shouldShowSelectedMode = !rightPanelMode && (selectedMemo || selectedTask);
+ const selectedItemType = selectedMemo ? "memo" : selectedTask ? "task" : null;

  const boardHeaderConfig = useMemo<HeaderControlPanelConfig | null>(() => {
```

---

### タスク2: boardHeaderConfig の条件分岐実装

**ファイル**: `apps/web/components/screens/board-detail-screen.tsx`
**位置**: 636-665行付近

**変更内容**: 基本設定オブジェクトから `showMemo`, `showTask`, `onMemoToggle`, `onTaskToggle`, `contentFilterRightPanelMode` を削除し、条件分岐で設定する

```diff
  const boardHeaderConfig = useMemo<HeaderControlPanelConfig | null>(() => {
    const config: HeaderControlPanelConfig = {
      currentMode: "board",
      rightPanelMode: headerRightPanelMode,
      boardId,
      onBoardSettings: boardSettingsHandler,
      onBoardExport: handleExport,
      isExportDisabled: false,
-     showMemo: headerShowMemo,
-     showTask: headerShowTask,
-     onMemoToggle: handleMemoToggle,
-     onTaskToggle: handleTaskToggle,
-     contentFilterRightPanelMode: rightPanelMode,
      selectionMode,
      onSelectionModeChange: handleSelectionModeChange,
      onCsvImport: handleCsvImport,
      customTitle: boardName || "ボード詳細",
      normalCount: totalNormalCount,
      completedCount,
      deletedCount: totalDeletedCount,
      teamMode,
      teamId: teamId ?? undefined,
    };

+   // アイテム選択時のパネル制御モード
+   if (shouldShowSelectedMode) {
+     config.isSelectedMode = true;
+     config.showMemo = showMemo;
+     config.showTask = showTask;
+     config.onMemoToggle = handleMemoToggle;
+     config.onTaskToggle = handleTaskToggle;
+     config.contentFilterRightPanelMode = rightPanelMode;
+     config.listTooltip = showMemo
+       ? "一覧パネルを非表示"
+       : "一覧パネルを表示";
+     config.detailTooltip = showTask
+       ? "詳細パネルを非表示"
+       : "詳細パネルを表示";
+     config.selectedItemType = selectedItemType;
+   } else {
+     // 通常モード
+     config.showMemo = headerShowMemo;
+     config.showTask = headerShowTask;
+     config.onMemoToggle = handleMemoToggle;
+     config.onTaskToggle = handleTaskToggle;
+     config.contentFilterRightPanelMode = rightPanelMode;
+   }

    if (teamMode) {
      config.showComment = showComment;
      config.onCommentToggle = handleCommentToggle;
    }

    return config;
```

---

### タスク3: 依存配列の更新

**ファイル**: `apps/web/components/screens/board-detail-screen.tsx`
**位置**: 667-688行付近

**変更内容**: useMemo の依存配列に追加

```diff
    return config;
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [
    headerRightPanelMode,
    boardId,
    // boardSettingsHandler, // 関数は除外
    // handleExport, // 関数は除外
    headerShowMemo,
    headerShowTask,
    // handleMemoToggle, // 関数は除外
    // handleTaskToggle, // 関数は除外
    rightPanelMode,
    selectionMode,
    // handleSelectionModeChange, // 関数は除外
    // handleCsvImport, // 関数は除外
    boardName,
    totalNormalCount,
    completedCount,
    totalDeletedCount,
    teamMode,
    teamId,
    showComment,
    // handleCommentToggle, // 関数は除外
+   shouldShowSelectedMode,
+   selectedItemType,
+   showMemo,
+   showTask,
  ]);
```

---

## 🧪 テストシナリオ

### シナリオ1: メモ選択時の動作確認

1. 個人ボード詳細を開く
2. メモを1つクリックして選択
3. ヘッダーのContentFilterを確認
   - 左ボタン: 一覧アイコン、ツールチップ「一覧パネルを表示/非表示」
   - 右ボタン: メモアイコン（緑）、ツールチップ「詳細パネルを表示/非表示」
4. 左ボタンをクリック → 一覧パネルの表示/非表示が切り替わる
5. 右ボタンをクリック → 詳細パネルの表示/非表示が切り替わる

### シナリオ2: タスク選択時の動作確認

1. 個人ボード詳細を開く
2. タスクを1つクリックして選択
3. ヘッダーのContentFilterを確認
   - 左ボタン: 一覧アイコン、ツールチップ「一覧パネルを表示/非表示」
   - 右ボタン: タスクアイコン（青）、ツールチップ「詳細パネルを表示/非表示」
4. 左ボタンをクリック → 一覧パネルの表示/非表示が切り替わる
5. 右ボタンをクリック → 詳細パネルの表示/非表示が切り替わる

### シナリオ3: 選択解除時の動作確認

1. メモまたはタスクを選択した状態
2. ×ボタンで選択解除
3. ヘッダーのContentFilterが通常モードに戻る
   - 左ボタン: メモアイコン、ツールチップ「メモを非表示」
   - 右ボタン: タスクアイコン、ツールチップ「タスクを非表示」

---

## 📚 関連ドキュメント

- `.claude/開発メモ/技術スタック.md` - プロジェクト構成
- `.claude/CLAUDE.md` - 開発ルール
- チーム側実装: `apps/web/components/screens/board-detail-screen-3panel.tsx`

---

**作成日**: 2025-01-18
**最終更新日**: 2025-01-18
